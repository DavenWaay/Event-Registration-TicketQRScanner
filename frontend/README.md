# Event Registration Frontend (scaffold)

Quick start (inside `frontend`):

Windows (PowerShell):

```powershell
cd "Event-Registration-TicketQRScanner/frontend"
npm install
npm run dev
```

The frontend expects backend at `http://localhost:4000`.
